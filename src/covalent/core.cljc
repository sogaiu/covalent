(ns covalent.core
  (:require
   #?(:cljs
      [covalent.ws :as cw]
      :default
      [covalent.tcp :as ct])))

(comment

  ;; for cljs, see covalent.ws

  ;; for clj and cljr, see covalent.tcp
  
  )
